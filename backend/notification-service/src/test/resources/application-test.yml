spring:
  application:
    name: notification-service-test

  # Embedded Redis for testing (optional)
  redis:
    host: localhost
    port: 6379

  # Test RabbitMQ Configuration
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  # Test Kafka Configuration
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-service-test

  security:
    jwt:
      secret: test-secret-key-that-is-long-enough-for-testing-purposes
      expiration: 3600000

logging:
  level:
    root: WARN
    com.fooddelivery: DEBUG

notification:
  websocket:
    allowed-origins: "http://localhost:3000"
